# Core (required)
fastapi>=0.110
uvicorn[standard]>=0.29
python-docx>=1.1
lxml>=5.0
pdfplumber>=0.11
python-multipart>=0.0.9
pymupdf>=1.24        # extract embedded images from PDFs
pillow>=10.0         # normalize/thumbnail those images

# Local NER (Layer 3) — ORG/PERSON/PLACE suggestions, fully offline.
# On macOS, install with prebuilt wheels to avoid a source build:
#   pip install --only-binary=:all: spacy
spacy>=3.7
# The English model (ships as a pip wheel):
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# On-device OCR for screenshots/images (macOS only — Apple Vision framework).
# The app degrades gracefully without these on non-Mac builds.
pyobjc-framework-Vision>=10.0 ; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0 ; sys_platform == "darwin"

# Desktop app (native window + .app packaging; see dactful.spec, build_dmg.sh)
pywebview>=5.0 ; sys_platform == "darwin"
pyinstaller>=6.0

# Dev
pytest>=8.0
